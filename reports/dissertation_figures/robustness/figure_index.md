# Robustness Figure Index

This index covers the robustness/failure-analysis figures added in PR #22. For the final short thesis set, use
`top_figures_for_thesis.md` alongside the main PR #21 figures. The final QA pass supports this narrative:
Mahalanobis feature distance is the strongest quantitative gatekeeper, PatchCore L3 is most useful as a localizable
complement, `val_id_quantile_95` is the balanced ID-calibrated prototype threshold, text watermark is the hardest
Mahalanobis subtype, artifact severity curves are not universally monotonic, and all results remain proof-of-concept
rather than clinical deployment validation.

| File | Recommended section | Suggested caption |
| --- | --- | --- |
| figure_bootstrap_ci_main_metrics.png | Results | Bootstrap AUROC uncertainty. Generated for the unsupervised FAF OOD gatekeeper; training is ID-only and OOD labels are used only for evaluation grouping. The ID test set is synthetic fallback, not real clinical FAF validation. |
| figure_bootstrap_ci_fpr95.png | Safety | Bootstrap FPR@95%TPR uncertainty. Generated for the unsupervised FAF OOD gatekeeper; training is ID-only and OOD labels are used only for evaluation grouping. The ID test set is synthetic fallback, not real clinical FAF validation. |
| figure_train_size_sensitivity_auroc.png | Results | Train-size AUROC sensitivity. Generated for the unsupervised FAF OOD gatekeeper; training is ID-only and OOD labels are used only for evaluation grouping. The ID test set is synthetic fallback, not real clinical FAF validation. |
| figure_train_size_sensitivity_fpr95.png | Safety | Train-size FPR@95 sensitivity. Generated for the unsupervised FAF OOD gatekeeper; training is ID-only and OOD labels are used only for evaluation grouping. The ID test set is synthetic fallback, not real clinical FAF validation. |
| figure_threshold_policy_tradeoff.png | Safety | Threshold policy trade-off. Generated for the unsupervised FAF OOD gatekeeper; training is ID-only and OOD labels are used only for evaluation grouping. The ID test set is synthetic fallback, not real clinical FAF validation. |
| figure_id_rejection_vs_ood_recall.png | Safety | ID rejection versus OOD recall. Generated for the unsupervised FAF OOD gatekeeper; training is ID-only and OOD labels are used only for evaluation grouping. The ID test set is synthetic fallback, not real clinical FAF validation. |
| figure_threshold_policy_by_ood_type.png | Safety | OOD-type recall by threshold policy. Generated for the unsupervised FAF OOD gatekeeper; training is ID-only and OOD labels are used only for evaluation grouping. The ID test set is synthetic fallback, not real clinical FAF validation. |
| figure_artifact_severity_scores.png | Stress tests | Artifact severity score response. Generated for the unsupervised FAF OOD gatekeeper; training is ID-only and OOD labels are used only for evaluation grouping. The ID test set is synthetic fallback, not real clinical FAF validation. |
| figure_artifact_severity_reject_rate.png | Stress tests | Artifact severity reject rate. Generated for the unsupervised FAF OOD gatekeeper; training is ID-only and OOD labels are used only for evaluation grouping. The ID test set is synthetic fallback, not real clinical FAF validation. |
| figure_artifact_severity_examples.png | Stress tests | Visual examples of generated artifact severities. Generated for the unsupervised FAF OOD gatekeeper; training is ID-only and OOD labels are used only for evaluation grouping. The ID test set is synthetic fallback, not real clinical FAF validation. |
| figure_method_disagreement_matrix.png | Failure analysis | Method disagreement case counts. Generated for the unsupervised FAF OOD gatekeeper; training is ID-only and OOD labels are used only for evaluation grouping. The ID test set is synthetic fallback, not real clinical FAF validation. |
| figure_method_disagreement_examples.png | Failure analysis | Representative method disagreement examples. Generated for the unsupervised FAF OOD gatekeeper; training is ID-only and OOD labels are used only for evaluation grouping. The ID test set is synthetic fallback, not real clinical FAF validation. |
| figure_failure_case_grid.png | Failure analysis | Representative failure cases. Generated for the unsupervised FAF OOD gatekeeper; training is ID-only and OOD labels are used only for evaluation grouping. The ID test set is synthetic fallback, not real clinical FAF validation. |
| figure_false_positive_id_examples.png | Failure analysis | ID false-positive examples. Generated for the unsupervised FAF OOD gatekeeper; training is ID-only and OOD labels are used only for evaluation grouping. The ID test set is synthetic fallback, not real clinical FAF validation. |
| figure_false_negative_ood_examples.png | Failure analysis | OOD false-negative examples. Generated for the unsupervised FAF OOD gatekeeper; training is ID-only and OOD labels are used only for evaluation grouping. The ID test set is synthetic fallback, not real clinical FAF validation. |
| figure_feature_space_pca_by_ood_type.png | Feature interpretation | Feature-space PCA by OOD type. Generated for the unsupervised FAF OOD gatekeeper; training is ID-only and OOD labels are used only for evaluation grouping. The ID test set is synthetic fallback, not real clinical FAF validation. |
| figure_feature_space_pca_by_subtype.png | Feature interpretation | Feature-space PCA by subtype. Generated for the unsupervised FAF OOD gatekeeper; training is ID-only and OOD labels are used only for evaluation grouping. The ID test set is synthetic fallback, not real clinical FAF validation. |
| figure_feature_space_pca_by_score.png | Feature interpretation | Feature-space PCA by Mahalanobis score. Generated for the unsupervised FAF OOD gatekeeper; training is ID-only and OOD labels are used only for evaluation grouping. The ID test set is synthetic fallback, not real clinical FAF validation. |
| figure_runtime_vs_performance.png | Deployment practicality | Runtime versus performance. Generated for the unsupervised FAF OOD gatekeeper; training is ID-only and OOD labels are used only for evaluation grouping. The ID test set is synthetic fallback, not real clinical FAF validation. |
| figure_method_tradeoff_table.png | Deployment practicality | Method trade-off table. Generated for the unsupervised FAF OOD gatekeeper; training is ID-only and OOD labels are used only for evaluation grouping. The ID test set is synthetic fallback, not real clinical FAF validation. |
| figure_subtype_influence.png | Results | Subtype influence diagnostics. Generated for the unsupervised FAF OOD gatekeeper; training is ID-only and OOD labels are used only for evaluation grouping. The ID test set is synthetic fallback, not real clinical FAF validation. |
